# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/
.idea/
.private/

# This module's built binary
gofir

# I'm not sure what these are tbh, the name comes from the sql.Open connection handle in duckdb.go
in-proc-duckdb
in-proc-duckdb.wal

# we only want to commit the zipped version, but the app unzips it on startup
duckdb_extensions/httpfs.duckdb_extension
duckdb_extensions/parquet.duckdb_extension
